import React, { useState, useEffect } from 'react'; import axios from './api'; const App = () => { const [items, setItems] = useState([]); const [input, setInput] = useState(''); useEffect(() => { fetchItems(); }, []); const fetchItems = async () => { const res = await axios.get('/items'); setItems(res.data); }; const addItem = async () => { if (input) { await axios.post('/items', { name: input }); setInput(''); fetchItems(); } }; const deleteItem = async (id) => { await axios.delete(`/items/${id}`); fetchItems(); }; return (<div><h1>Todolist</h1><input value={input} onChange={(e) => setInput(e.target.value)} /><button onClick={addItem}>Add</button><ul>{items.map(item => (<li key={item._id}>{item.name} <button onClick={() => deleteItem(item._id)}>Delete</button></li>))}</ul></div>); }; export default App;